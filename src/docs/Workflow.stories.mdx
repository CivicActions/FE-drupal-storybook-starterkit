import { Meta } from '@storybook/addon-docs';

<Meta title="Documentation/Workflow" />

# Workflow

This a page where workflow is explained or something like that...

Here is a code sample:

```js
const codeSample = {
  reaction: 'Cool!',
  effort: 'Easy!',
};
```
